# Selected AI Context

The accepted storage policy changed from full-bundle retention in every repository to a custody-separated model: `master-records` retains full bundles, while downstream repositories normally retain hashes, receipts, pointers, and documented active-custody exceptions.

Implementation is partial. The example intentionally keeps archive readiness false because master-records propagation verification remains unresolved.

This file is exact selected context for the example. It is not a complete transcript.
