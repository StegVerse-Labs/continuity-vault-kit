# Patch: StegDB Emoji Grammar (v1)

Adds:
- Emoji relationship grammar doc
- Emoji arrow-character lint (warn by default, strict optional)
- Config file under .stegdb/

Install:
- Copy contents into repo root
- Commit
