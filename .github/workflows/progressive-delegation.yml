name: Progressive Delegation Validation

on:
  pull_request:
    paths:
      - "schemas/delegation-policy.schema.json"
      - "schemas/relationship-declaration.schema.json"
      - "fixtures/delegation/**"
      - "fixtures/delegation-decisions/**"
      - "fixtures/delegation-onboarding/**"
      - "fixtures/delegation-lifecycle/**"
      - "fixtures/relationship-declarations/**"
      - "delegation/**"
      - "tools/validate_delegation_policy.py"
      - "tests/test_delegation_*.py"
      - "tests/test_relationship_declaration.py"
      - "docs/PROGRESSIVE_DELEGATION_AND_FAIR_AGENCY.md"
      - "docs/PROGRESSIVE_DELEGATION_IMPLEMENTATION_STATUS.md"
      - ".github/workflows/progressive-delegation.yml"
  push:
    branches: ["main"]
    paths:
      - "schemas/delegation-policy.schema.json"
      - "schemas/relationship-declaration.schema.json"
      - "fixtures/delegation/**"
      - "fixtures/delegation-decisions/**"
      - "fixtures/delegation-onboarding/**"
      - "fixtures/delegation-lifecycle/**"
      - "fixtures/relationship-declarations/**"
      - "delegation/**"
      - "tools/validate_delegation_policy.py"
      - "tests/test_delegation_*.py"
      - "tests/test_relationship_declaration.py"
      - "docs/PROGRESSIVE_DELEGATION_AND_FAIR_AGENCY.md"
      - "docs/PROGRESSIVE_DELEGATION_IMPLEMENTATION_STATUS.md"
      - ".github/workflows/progressive-delegation.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Compile delegation surface
        run: |
          python -m py_compile delegation/decision.py delegation/onboarding.py delegation/relationship.py tools/validate_delegation_policy.py
      - name: Validate delegation policies
        run: |
          python tools/validate_delegation_policy.py fixtures/delegation/*.json
      - name: Run authority, onboarding, and relationship tests
        run: |
          python -m unittest tests.test_delegation_decision tests.test_delegation_cases tests.test_delegation_onboarding tests.test_relationship_declaration -v
