# KnowledgeVault Cross-Edge Selection Mirror Handoff

Status: ACTIVE — SOURCE INTEGRATION VALIDATED
Repository: StegVerse-Labs/continuity-vault-kit
Integration: StegVerse-Labs/StegTalk ST-031 + StegVerse-Labs/StegWhisper SW-013

## Source of truth

This handoff is the task-specific source of truth for durable KnowledgeVault hosting of StegTalk cross-edge selection evidence. The repository-wide installation/source state remains governed by `CONTINUITY_VAULT_KIT_MIRROR_HANDOFF.md`.

## Implemented source state

KnowledgeVault provides `_System/Execution/{Attempts,Extensions,Receipts,Recovery}` through `execution/vault_store.py`; a matching structure exists in the connected KnowledgeVault instance.

Cross-edge artifacts:

```text
schemas/cross-edge-selection-receipt.schema.json
tests/test_cross_edge_selection_receipt.py
execution/vault_store.py
.github/workflows/execution-recovery.yml
```

The canonical receipt binds attempt/policy/posture/recipient state, candidate-set hash, selected edge/bearer, score vector, ordered fallbacks, exclusions, advertisement hash, decision time, multipath/remote-edge policy, and selection hash.

## Observed executable integration proof

The current KnowledgeVault implementation has now been exercised together with the actual StegTalk ST-031 resolver and actual StegWhisper v0.2 messenger-preference output.

Observed validation:

```text
StegVerse-org/StegVerse-SDK PR #55
workflow run 32602863793
Python 3.9  SUCCESS
Python 3.11 SUCCESS
Python 3.12 SUCCESS

StegVerse-Labs/StegWhisper PR #15
workflow run 32602979304
full StegWhisper -> ST-031 -> KnowledgeVault source integration SUCCESS
```

Pinned source exercised by those proofs:

```text
StegTalk: 2361d13ea09818f17aef5239ebf4771a161a0dc7
continuity-vault-kit: 35e6d7ad881e0dea60ba191c49dfd4fba86e3fd7
```

The tests proved that the real ST-031 selection receipt and lease/attempt state can be persisted through `KnowledgeVaultExecutionStore`, a new store instance reconstructs them after restart, ambiguous post-dispatch state suppresses fallback, confirmed no-side-effect failure permits the exact ordered fallback, and StegWhisper remote-edge policy is enforced by ST-031.

## Connected KnowledgeVault evidence

The connected personal KnowledgeVault already contains:

```text
_System/Execution/
    Attempts/
    Extensions/
    Receipts/
    Recovery/
```

A durable validation receipt named `ST031_Communication_Integration_Validation_2026-08-22` is now located in the connected `_System/Execution/Receipts` folder. It records the validated commits, workflow runs, selection hash, demonstrated invariants, authority boundary, and remaining runtime proof.

This connected-vault document is validation evidence. It is not falsely classified as a production execution receipt generated by a live bearer transaction.

## Authority boundary

```text
KnowledgeVault = durable attempt/selection/recovery truth
StegWhisper = posture + user constraints
StegTalk ST-031 = admissibility + scoring + edge/bearer selection + lease/fallback logic
Edge device = ephemeral capability advertisement + execution
SDK = non-authorizing demonstration/conformance boundary
```

Capability never grants execution authority. Selection never grants new user authority. Ambiguous delivery state never becomes permission to duplicate a side effect.

## Remaining activation boundary

Source integration and restart/reconstruction are now validated. Still open:

1. originate a runtime attempt against the connected KnowledgeVault rather than a temporary test vault;
2. persist its actual runtime selection receipt and lease into the connected KV execution streams;
3. advertise at least two actual admitted device edges;
4. execute through the selected real bearer and append delivery evidence;
5. restart/replace the selected edge and reconstruct the live attempt from connected KV without duplicate dispatch;
6. exercise ST-029 modem/SIM as a physical SMS edge;
7. only then claim applicable runtime/production activation.

## Archive posture

DO NOT archive as fully activated. The source/software integration is implemented and tested; physical/network execution and connected-vault live-attempt proof remain open.
